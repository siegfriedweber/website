{
  "private": true,
  "scripts": {
    "build": "pulp browserify --optimise --to dist/main.js && jsmin --overwrite dist/main.js"
  },
  "devDependencies": {
    "jsmin": "^1.0.1",
    "pulp": "^11.0.0"
  }
}
