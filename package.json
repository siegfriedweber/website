{
  "private": true,
  "scripts": {
    "build": "pulp browserify --optimise --to dist/main.js"
  },
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "pulp": "^10.0.0"
  }
}
