{
  "private": true,
  "scripts": {
    "build": "pulp browserify --optimise --to dist/main.js && jsmin --overwrite dist/main.js"
  },
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "jsmin": "^1.0.1",
    "pulp": "^10.0.0"
  }
}
